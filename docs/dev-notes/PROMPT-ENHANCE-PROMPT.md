# پرامپت ۳ از ۳: «بهبود پرامپت» زمزمه (بتا، برای چت نو)

می‌خوام یک کار اختیاری و بتا به زمزمه اضافه کنی: بعد از اینکه متن نهایی آماده شد، با یک دکمه همان متن به یک **پرامپت درست و حرفه‌ای** تبدیل شود. یعنی چیزی که با حرف زدن دیکته شده، به پرامپتی برسد که بشود مستقیم به یک ایجنت یا مدل دیگر داد.

**در هر چهار حالت در دسترس باشد** (زنده، جمع، کرسر، یادداشت) و در پنل رونویسی فایل هم. هر جا متنی آماده هست، این دکمه هم هست.

اول کل ریپو را بخوان، بعد پلن کوتاه بده، بعد بساز و تا تست و پوش برو. پوش مستقیم به main.

## وابستگی و هم‌زمانی (مهم، اول این را بخوان)

دو کار دیگر جلوتر از تو هستند و هر دو به main پوش می‌کنند: `NOTE-TOOL-PROMPT.md` (ابزار چهارم و کلاس `ZFinalPass` که تماس با جمینای را انجام می‌دهد) و `FINAL-PASS-PROMPT.md` (همان پاس روی سه حالت فعلی).

- کد تازه‌ات را در **فایل خودش** بنویس (`app/Sources-objc/enhance.m` و اعلان در `zemzeme.h`). فایل‌های مشترک (`app.m`، `ui.m`، `batchui.m`) را فقط با کمترین افزودن دست بزن، هیچ بازآرایی و هیچ ریفکتوری.
- اگر `ZFinalPass` قبل از تو رسیده بود، **همان را استفاده کن** و لایه‌ی انتقال تازه نساز. اگر نرسیده بود، یک انتقال نازک بساز که بعدا راحت به آن وصل شود.
- کار تو تنها کاری از این سه است که **به صدا نیازی ندارد**: ورودی‌اش متن آماده است، پس می‌تواند مستقل از آن دو پیش برود.
- قبل از پوش `git pull --rebase` بزن.

## وضعیت ریپو (دوباره کشفش نکن)

- اپ نیتیو منوبار، ObjC در `app/Sources-objc/`، بیلد با `bash app/build.sh`. سوئیفت روی این مک بیلد نمی‌شود؛ `app/swift-port/` پارک است.
- **حالت‌ها**: `ZMode` در `zemzeme.h:46` با `ZModeLive=0`، `ZModeCollect=1`، `ZModeCursor=2`، و `ZModeNote=3` که کار اول اضافه‌اش می‌کند.
- متن آماده در دو ادیتور می‌نشیند و همین دو نقطه‌ی اتصال تو هستند: ادیتور پنل در `ui.m` (`_btnPolish` در خط ~۵۵۴، الان فقط در حالت جمع دیده می‌شود) و ادیتور پنل رونویسی فایل در `batchui.m` (دکمه‌ی «پاس نهایی»، خط ~۷۴۴).
- در حالت زنده و کرسر ادیتوری روی صفحه نیست. آنجا خروجی تو فقط به کلیپ‌بورد می‌رود، با یک پیام کوتاه روی خط وضعیت. **هیچ‌وقت به اپ مقصد تایپ نمی‌شود**: قاعده‌ی «بازویرایش ممنوع» ریپو.
- تاگل‌های ماندگار در `ZSettings` (`core.m`) و آیتم منو در `app.m` (الگوی `menuNeedsUpdate` و نمونه‌ی `polishEnabled` در خطوط ۳۳۴ و ۴۷۷).
- کارت میان‌برها در `cheatsheet.m`. هر میان‌بر تازه باید آنجا هم بیاید، وگرنه کاربر پیدایش نمی‌کند.
- کلید API در Keychain، سرویس `zemzeme-gemini`. نه در plist، نه در `.env`.

## اندازه‌گیری‌های انجام‌شده (دوباره تحقیق نکن)

- مدل: `gemini-3.6-flash` را **پین کن**. `gemini-flash-latest` هم کار می‌کند ولی alias بی‌خبر جابه‌جا می‌شود.
- اندپوینت: `POST https://generativelanguage.googleapis.com/v1beta/interactions` با هدر `x-goog-api-key`. بدنه: `{"model":..., "system_instruction":..., "input":[{"type":"text","text":...}], "generation_config":{"thinking_level":"low"}}`.
- مقادیر مجاز `thinking_level`: `minimal`، `low`، `high`. (`none` و `off` خطای ۴۰۰ می‌دهند.)
- **اینجا `low` بزن، نه `minimal`.** فرق این کار با پاس تمیزکاری همین است: کاربر خودش دکمه را زده و منتظر است، پس چند ثانیه بیشتر می‌ارزد و کیفیت ساختار مهم‌تر است. عدد واقعی را بعد از ساخت با هر دو بگیر و در README بنویس.
- پاسخ: متن داخل `steps` است. شکل دقیق مستند نیست، پس در کل JSON هر کلید `text` را جمع کن و هر دیکشنری با `thought: true` را رد کن. مصرف در `usage` با کلیدهای `total_input_tokens` و `total_output_tokens` و `total_thought_tokens`.
- پیاده‌سازی مرجع کاری (پایتون، فقط stdlib) در `~/Projects/tools/utils/zemzeme-lab/lab.py` است. تابع `measure()` اش را برای دروازه پورت کن، با هر دو باگی که در آن درست شده: نیم‌فاصله باید به فاصله تبدیل شود نه حذف، و تطبیق باید بی‌فاصله هم امتحان شود.
- هر تماس باید تلاش دوباره داشته باشد؛ یک قطعی شبکه در آزمایش کل اجرا را کشت.

## پرامپت خودِ بهبوددهنده (پیش‌نویس، تیونش کن)

این را در `app/prompts/enhance.md` بگذار، کنار دو پرامپت موجود (`verbatim.md` و `polish.md`)، و مثل آن‌ها در `app/build.sh` به `Contents/Resources` کپی شود تا بی‌بیلد قابل ویرایش باشد.

```
تو یک مهندس پرامپت هستی. ورودی، متنی است که کاربر با حرف زدن دیکته کرده و
منظورش این بوده که کاری را به یک مدل یا یک ایجنت بسپارد. کارَت این است که همان
خواسته را به یک پرامپت روشن و کامل تبدیل کنی. نه خلاصه‌اش کنی، نه بزرگش کنی.

قاعده‌ها، به ترتیب اهمیت:

۱. هیچ خواسته‌ای حذف نمی‌شود. هر عدد، اسم، مسیر فایل، نام ابزار، نسخه، محدودیت و
   شرط که گفته شده باید در پرامپت نهایی باشد، مو‌به‌مو.
۲. هیچ خواسته‌ای اضافه نمی‌شود. چیزی که کاربر نگفته، تو از خودت درنیاور: نه
   تکنولوژی، نه معیار، نه مرحله. حدس زدنِ نیت، بدترین خطای این کار است.
۳. چیزی که کاربر نگفته ولی برای انجام کار لازم است، حذف یا حدس نمی‌شود، بلکه در
   بخش آخر زیر عنوان «مفروضات و سوال‌های باز» فهرست می‌شود تا کاربر خودش پرش کند.
۴. ساختار را از خودِ خواسته دربیار، نه از یک قالب ثابت:
   - اگر کار ساختنی یا کدنویسی است: خواسته در یک جمله، بعد کانتکست، بعد کاری که
     باید بشود، بعد محدودیت‌ها و چیزهایی که نباید دست بخورند، بعد معیار پذیرش.
   - اگر کار نوشتنی یا فکری است: نقش، خواسته، مخاطب، لحن، شکل خروجی، معیار خوب بودن.
   قالب را زورکی پر نکن. بخشی که حرفی برایش نیست، نیاید.
۵. با مارک‌داون بنویس: تیتر کوتاه، بولت، مرحله‌ی شماره‌دار. متن یک‌دست و بی‌سرفصل،
   پرامپت بدی است.
۶. فیلر و من‌من و تکرار برداشته می‌شود. خودتصحیحی گوینده اعمال می‌شود: اگر گفت
   «سه‌شنبه، نه ببخشید چهارشنبه»، فقط چهارشنبه می‌ماند.
۷. زبان خروجی همان زبان ورودی است، مگر خلافش گفته شود.
۸. کوتاه‌ترین شکلِ کامل. اگر پرامپت نهایی از دو برابر ورودی بلندتر شد، یعنی داری
   حرف اضافه می‌زنی.
۹. خروجی فقط خودِ پرامپت است. نه توضیح کارت، نه «بفرمایید»، نه علامت کدِ دور
   کل متن.
```

## قواعد سخت پیاده‌سازی

- **هیچ‌وقت خودکار نیست.** فقط با دکمه یا میان‌بر، و روی متنی که از قبل آماده شده. هیچ‌وقت وسط سشن و هیچ‌وقت روی تکه‌های در جریان.
- **متن اصلی همیشه می‌ماند.** یک میان‌بر بین «متن دیکته» و «پرامپت بهبودیافته» بچرخد. کاربری که بتواند مقایسه کند به فیچر اعتماد می‌کند؛ کاربری که نتواند، نه.
- **برچسب بتا واقعی باشد**: تاگل جدا در منو با کلمه‌ی «بتا»، پیش‌فرض خاموش. جفت قبل و بعد در `enhance.log` کنار `polish.log` (به `.gitignore` هم اضافه‌اش کن).
- **بازویرایش ممنوع.** خروجی این کار هیچ‌وقت به اپ مقصد تایپ نمی‌شود. فقط ادیتور و کلیپ‌بورد.
- خطا یا شبکه‌ی قطع یعنی هیچ اتفاقی نیفتاد: متن قبلی سر جایش، یک پیام کوتاه روی خط وضعیت.
- سشن انگلیسی هم پشتیبانی شود؛ پاس مکانیکی فارسی روی خروجی انگلیسی اجرا نشود.

## دروازه

- **عدد و لاتین و مسیر فایل، تحمل صفر.** هر رقم، توکن لاتین و مسیر فایلِ متن ورودی باید در پرامپت خروجی باشد. نبود، خروجی رد می‌شود و متن قبلی می‌ماند.
- **سقف طول**: خروجی بیشتر از ۲٫۵ برابر ورودی یعنی پرحرفی. رد کن و یک بار با تاکید بر کوتاهی دوباره بفرست.
- **بخش مفروضات اجباری نیست ولی جای دیگری نباید باشد**: اگر مدل حدسی زد و آن را داخل تنِ پرامپت به شکل خواسته نوشت، غلط است. در تست دنبال همین بگرد.
- رد شد؟ یک بار دوباره با پرامپت سخت‌گیرتر؛ باز رد شد، تسلیم و متن قبلی بماند.

## تست و تحویل

- ست طلایی از پنج شش دیکته‌ی واقعی. یکی‌شان همان `voice.ogg` در `~/Projects/tools/utils/zemzeme-lab` است که خروجی نهایی‌اش هم آماده است.
- هر کیس این‌ها را چک کند: همه‌ی عددها و اسم‌ها و مسیرها مانده‌اند؛ هیچ خواسته‌ای اضافه نشده؛ ساختار مارک‌داون هست؛ طول زیر سقف است؛ حدس‌ها فقط در بخش مفروضات‌اند؛ زبان عوض نشده.
- یک کیس منفی هم لازم است: دیکته‌ای که خواسته‌ی مشخصی ندارد (فقط بلندبلند فکر کردن). خروجی درست آنجا یک پرامپت الکی نیست، بلکه پرسیدنِ همان ابهام در بخش مفروضات است.
- زمان و هزینه را با `minimal` و `low` بگیر و در README بنویس.
- بیلد، اجرای واقعی، بخش کوتاه در README (چه می‌کند، چرا بتاست، تاگل و میان‌بر کجاست)، کامیت و پوش به main.

## معیار پذیرش

یک خواسته‌ی کاری را با حرف زدن بگو، پرآشفته و با فیلر و یک خودتصحیحی. دکمه را بزن. چیزی که می‌آید باید پرامپتی باشد که بشود همان لحظه کپی کرد و به یک ایجنت داد: با ساختار، بی‌فیلر، با همه‌ی عددها و اسم‌های اصلی، بی‌هیچ خواسته‌ی از خود درآورده، و با ابهام‌ها فهرست‌شده در آخر. تاگل خاموش، هیچ رفتاری عوض نشود.
