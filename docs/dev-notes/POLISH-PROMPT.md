# پرامپت ساخت پاس ویرایش خودکار فارسی (برای چت جدید)

می‌خوام به زمزمه (همین ریپو) یه «پاس ویرایش خودکار» اضافه کنی: هر تیکه متن که قطعی می‌شه (خاکستری به سفید)، قبل از نشستن سر کرسر، آنی و بی‌سروصدا تمیز بشه. اول کل ریپو رو بخون، بعد پلن کوتاه بده، بعد بساز و تا تست و پوش برو.

## وضعیت فعلی ریپو (بازطراحی انجام شده 2026-07-24؛ دوباره کشفش نکن)

- محصول، اپ نیتیو منوباره: `Zemzeme.app`، سورس ObjC توی `app/Sources-objc/` (۸ فایل)، بیلد با `bash app/build.sh`. ریپو `github.com/SMKeramati/zemzeme`، برنچ main، پوش مستقیم.
- **سوئیفت روی این مک بیلد نمی‌شه** (CLT با همه SDK ها ناسازگاره؛ حتی `import Foundation` فیل می‌شه). به `app/swift-port/` دست نزن؛ فقط پورت پارک‌شده‌ست. هر کد اپ که می‌نویسی ObjC و با clang توی همون build.sh کامپایل می‌شه.
- **نقطه اتصال دقیق:** `ZSession` توی `app/Sources-objc/ui.m`، متد `engineFinal:`. هر تکه قطعی از هر دو موتور (گوگل مستقیم و صفحه کروم) از همین یک نقطه رد می‌شه، قبل از صف و درج و قبل از `transcript` (که کپی پایانی کلیپ‌بورد ازش ساخته می‌شه). خروجی پاس باید به هر دو برسه.
- `sessions/` خام می‌مونه (طلای تست آینده‌ست): متن خام مثل الان توی فایل سشن ذخیره بشه، متن پاس‌خورده فقط به درج و کلیپ‌بورد بره. جفت قبل/بعد هر تکه توی `polish.log` (کنار `app.log`، به `.gitignore` هم اضافه‌ش کن).
- سرویس پاس: پایتون لوکال گرم (مدل‌ها یه بار موقع بوت لود). یا `POST /polish` روی همین `serve.py` (پورت ۱۷۶۳۵، فقط stdlib بوده تا حالا) یا یه دیمن جدای `polish.py`؛ انتخاب با تو، ولی اپ باید خودش بالا بیاردش: الگوی آماده spawn و ping توی `engines.m` هست (`ZChromeRelayEngine`: متدهای `ping` و `spawnServer`). یه کلاس کوچیک `ZPolish` جدا بنویس؛ موتور ۱ نباید به کد موتور ۲ وابسته بشه.
- تماس ObjC: async با سقف سخت ۳۰۰ میلی‌ثانیه؛ جواب نیومد یا خطا داد، همون تکه خام درج می‌شه و دیگه هیچ‌وقت جایگزین نمی‌شه (retro-edit ممنوع، متن رفته توی اپ مقصد). موقع شروع سشن یه warmup هم بزن که تکه اول سرد نخوره.
- زبان سشن توی `ZSettings.shared.lang` هست (`fa-IR` یا `en-US`): انگلیسی یعنی بای‌پس کامل پاس. توی فارسی هم تکه تمام‌لاتین دست‌نخورده رد بشه.
- تاگل ماندگار (پیش‌فرض روشن): کلید جدید توی `ZSettings` (فایل `core.m`) + آیتم منو توی `app.m` (الگوی `menuNeedsUpdate` رو ببین). خاموش یعنی دقیقا رفتار خام فعلی.
- پایتون: `/opt/homebrew/bin/python3`. کاربر فارسی دیکته می‌کنه، گاهی انگلیسی. نمونه واقعی دیکته‌ها (خطاهای واقعی STT) توی `sessions/*.txt` هست، هم قدیمی‌ها هم `app-*.txt` جدیدها.

## چی می‌سازی: فقط اصلاح‌های قطعی و بی‌ریسک

سه جزء، هر سه لوکال و رایگان (تحقیق شده 2026-07-24، دوباره تحقیق نکن):

1. **نرمال‌سازی** با `piraye` (pip، فعال و به‌روز): نیم‌فاصله، یکدست‌سازی حروف عربی/فارسی، ارقام فارسی، فاصله‌گذاری علائم.
2. **نقطه‌گذاری** با مدل `MohammadJRanjbar/parsbert-persian-punctuation` (مخصوص فارسی، real-time). اگه به هر دلیل جواب نداد، فال‌بک: `1-800-BAD-CODE/xlm-roberta_punctuation_fullstop_truecase` (فارسی تاییدشده، ONNX داره). مدل یه بار دانلود و کش می‌شه؛ بعدش کاملا آفلاین.
3. **املای محافظه‌کارانه** با hunspell و دیکشنری `b00f/lilak`: فقط وقتی کلمه غلطه و دقیقا یه پیشنهاد با فاصله ویرایشی کم وجود داره اصلاح کن؛ کوچک‌ترین ابهام یعنی دست نزن.

## قواعد سخت (نقض هیچ‌کدوم قابل قبول نیست)

- **هیچ LLM مولدی توی این پاس نیست.** مدرک: تنها مطالعه اصلاح STT فارسی (arXiv 2512.17247) نشون داده LLM خام خطا رو تقریبا دو برابر می‌کنه. این پاس فقط قواعد و مدل‌های طبقه‌بندی کوچیکه.
- **معنا هیچ‌وقت عوض نمی‌شه.** لیست سفید عملیات: فاصله و نیم‌فاصله، یکدست‌سازی نویسه، ارقام، درج علائم نقطه‌گذاری، اصلاح املایی تک‌پیشنهادی. هیچ جابجایی، حذف یا بازنویسی کلمه. اصلاح خطاهای شنیداری (مثل «اکاسی» به جای «عکاسی») خارج از اسکوپه چون بدون درک معنا ریسکیه.
- **بودجه تاخیر:** هدف زیر ۱۵۰ میلی‌ثانیه برای هر تیکه، سقف سخت ۳۰۰. دیر شد؟ همون متن خام رد بشه؛ تسمه‌نقاله هیچ‌وقت منتظر پاس نمی‌مونه.
- **ایدمپوتنت:** پاس روی متنِ پاس‌خورده باید no-op باشه.
- **به متن ویرایش‌شده کاربر دست نزن:** فقط تیکه تازه قطعی‌شده پاس می‌خوره، هیچ‌وقت متن قبلی دوباره پردازش نمی‌شه.
- وابستگی‌ها حداقلی و توجیه‌شده: piraye، hunspell (یا بایندینگ pyhunspell)، و یه رانتایم اینفرنس (ترجیحا onnxruntime به جای torch کامل اگه شدنیه). بیشتر از این نه.

## تست و تحویل

- ست طلایی: حدود ۳۰ جفت «قبل/بعد» از دل `sessions/*.txt` واقعی + چند کیس مصنوعی (نیم‌فاصله «می‌شود»، ارقام، جمله بی‌نقطه، کلمه با یه غلط روشن، متن انگلیسی دست‌نخورده، متن پاس‌خورده که باید no-op باشه). اجرا با یه دستور، مثلا `python3 tools/polish_test.py`؛ میانگین و بدترین تاخیر روی همین مک رو هم چاپ کنه.
- بعد از اتصال به اپ: بیلد، اجرای واقعی، و چک `polish.log` و `app.log`.
- README بخش کوتاه «پاس ویرایش» بگیره (چی اصلاح می‌شه، چی عمدا نمی‌شه، تاگل کجاست)، کامیت و پوش به main.
- اختیاری و آخر کار: صفحه وب مستقل (`index.html` بدون اپ) هم توی `appendFinal` از همون `/polish` استفاده کنه اگه سرور بالا بود؛ اولویت با مسیر اپه.

## معیار پذیرش

یه پاراگراف واقعی فارسی دیکته بشه: متن با نیم‌فاصله درست، ارقام فارسی، نقطه و ویرگول معقول سر کرسر بشینه، بدون هیچ تغییر معنا و بدون تاخیر محسوس. تاگل خاموش، رفتار خام برگرده. هر سه جزء آفلاین کار کنن. `sessions/` همچنان خام ذخیره بشه و `polish.log` جفت قبل/بعد داشته باشه.
