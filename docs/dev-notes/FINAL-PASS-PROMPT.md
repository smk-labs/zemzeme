# پرامپت ۲ از ۳: پاس نهایی با هوش مصنوعی روی سه حالت فعلی و پنل رونویسی فایل (برای چت نو)

می‌خوام همان پاس نهایی که برای حالت «یادداشت» ساخته شده، با یک دکمه روی سه حالت فعلی (زنده، جمع، کرسر) و روی پنل رونویسی فایل هم در دسترس باشد.

اول کل ریپو را بخوان، بعد پلن کوتاه بده، بعد بساز و تا تست و پوش برو. پوش مستقیم به main، بدون برنچ و بدون PR.

## وابستگی: اول این را چک کن

این کار روی زیرساختی سوار است که `NOTE-TOOL-PROMPT.md` ساخته: کلاس `ZFinalPass`، نوشتن صدا روی دیسک، دروازه‌ی کامل بودن، حالت `mechanical` روی `polish.py`، پرامپت‌ها در `Contents/Resources`، و خواندن کلید از Keychain.

- اگر `ZFinalPass` در `zemzeme.h` هست: **همان را استفاده کن**، لایه‌ی تازه نساز و شکل تماس با جمینای را دوباره ننویس.
- اگر نیست: یعنی آن کار هنوز نرسیده. **صبر کن یا اول همان را اجرا کن**؛ ساختن دوباره‌ی موتور یعنی دو نسخه‌ی ناهمگام از یک چیز.
- `git pull --rebase` قبل از پوش. ممکن است ایجنت سومی هم روی `PROMPT-ENHANCE-PROMPT.md` کار کند.

## آنچه واقعا می‌سازی (کوچک است، عمدا)

۱. **ضبط صدا در همه‌ی سشن‌ها**، نه فقط یادداشت. `ZMic` (در `zemzeme.h:266` و `audio.m`) از قبل PCM خام `s16le` مونو ۱۶ کیلوهرتز می‌دهد و `flac.m` انکودر دارد؛ همان مسیرِ نوشتن فایلی که یادداشت استفاده می‌کند را برای هر سشن روشن کن. هزینه‌اش لوکال و ناچیز است.
۲. **یک میان‌بر و یک دکمه‌ی آیکونی** برای «پاس نهایی» روی پنل. حرفی که در README اشغال نیست انتخاب کن و در کارت میان‌برهای `cheatsheet.m` هم ثبتش کن.
۳. **وصل کردن دکمه‌ی «پاس نهایی» پنل رونویسی فایل** (`batchui.m` خط ~۷۴۴، که الان `ZBatchPolishText` را صدا می‌زند) به موتور تازه. آنجا فایل صوتی اصلی در دست است، پس همان مسیر دومرحله‌ای کامل قابل اجراست.

## قاعده‌ی سختی که شکل کار را تعیین می‌کند

**بازویرایش ممنوع است.** این قاعده از قبل در ریپو هست و اینجا تعیین‌کننده می‌شود:

- **حالت زنده و کرسر**: متن از قبل سر کرسر اپ مقصد نشسته. خروجی پاس نهایی **هیچ‌وقت** به اپ مقصد تایپ نمی‌شود. فقط به ادیتور پنل و کلیپ‌بورد می‌رود، با یک پیام کوتاه که می‌گوید متن تمیز در کلیپ‌بورد است.
- **حالت جمع**: متن در ادیتور خود پنل است، پس جایگزینی آزاد است. اینجا طبیعی‌ترین جای این فیچر است.
- **پنل رونویسی فایل**: ادیتور خودش را دارد، جایگزینی آزاد.
- در هر سه حالت، متن قبلی باید با یک میان‌بر برگردد. کاربری که نتواند مقایسه کند به پاس اعتماد نمی‌کند.

## چیزهایی که دوباره کشف نکن

- مدل روی `gemini-3.6-flash` پین است. مسیر دومرحله‌ای (اول رونویسی مو‌به‌مو از صدا، بعد پاس متنی روی همان) با `thinking_level: minimal`. تماس یکجای «صدا تا متن نهایی» اندازه‌گیری شد و با `minimal` خروجی درهم داد؛ تکرارش نکن.
- روی ۶ دقیقه صدا: ~۲۱ ثانیه، ~۱۵ هزار توکن، دو درخواست.
- **متن خام STT فعلی را به عنوان ورودی پاس نده.** اندازه‌گیری روی ویس واقعی: آن متن ۷۷٪ کلمه‌ها را داشت و پاس متنی روی آن، صفر از ۱۱ تکه‌ی گم‌شده را برنگرداند. مرجع، خودِ صداست. اگر هم متن خام را کنار صدا گذاشتی، هیچ‌وقت «مرجع کامل بودن» صدایش نزن: در آزمایش همین جمله بازیابی را از ۹ به ۳ از ۱۱ آورد.
- آزمایشگاه و پیاده‌سازی مرجع پایتون در `~/Projects/tools/utils/zemzeme-lab` است (`lab.py` و `prompts/`). قبل از شروع بخوانش.

## قواعد سخت

- **پاس زنده‌ی حین دیکته دست‌نخورده می‌ماند.** بودجه‌ی ۳۰۰ میلی‌ثانیه‌ی `ZPolish` و قاعده‌ی «هیچ LLM مولدی در پاس زنده نیست» سر جایشان‌اند. این یک کار پایانی است، فقط با دست کاربر.
- **هیچ‌وقت خودکار نیست** در حالت زنده و کرسر. در حالت جمع می‌تواند تاگل «خودکار سر پایان» داشته باشد، پیش‌فرض خاموش.
- **هیچ چیز گم نمی‌شود.** صدا و متن خام و متن نهایی هر سه می‌مانند. `sessions/` مثل همیشه خام می‌ماند.
- مسیر خروج اپ منتظر پاس نمی‌ماند.
- تاگل ماندگار جدا در منو، و اگر کلید در Keychain نبود، آیتم غیرفعال با دلیل روشن.
- دروازه‌ی کامل بودن همان است که یادداشت استفاده می‌کند: عدد و لاتین با تحمل صفر، پوشش واژه‌های محتوایی، و در تردید متن خام.

## تست و تحویل

- سه حالت را واقعا اجرا کن: در زنده و کرسر مطمئن شو که **هیچ نویسه‌ای** بعد از پاس به اپ مقصد نمی‌رود (با یک ادیتور باز تست کن و متن مقصد را قبل و بعد مقایسه کن). در جمع مطمئن شو جایگزینی درست و برگشت‌پذیر است.
- پنل رونویسی فایل را روی یک فایل کوتاه اجرا کن و با خروجی قبلی (`ZBatchPolishText`) مقایسه کن.
- ضبط صدا در همه‌ی سشن‌ها: چک کن یک سشن ۱۰ دقیقه‌ای فایل سالم می‌دهد و حافظه بالا نمی‌رود.
- بخش کوتاه در README (کجا در دسترس است، در زنده و کرسر چرا فقط کلیپ‌بورد، تاگل و میان‌بر کجاست)، کامیت و پوش به main.

## معیار پذیرش

در حالت جمع یک پاراگراف دیکته شود، دکمه زده شود، متن تمیز جای متن قبلی بنشیند و با یک میان‌بر متن قبلی برگردد. در حالت زنده همان کار انجام شود و متن تمیز فقط در کلیپ‌بورد باشد، در حالی که متن اپ مقصد یک نویسه هم عوض نشده. تاگل خاموش، رفتار امروز عینا برگردد.
