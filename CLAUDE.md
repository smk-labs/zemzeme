# زمزمه: قانونِ کارِ روی این ریپو

اینجا فقط چیزی نوشته شده که **از خودِ کد درنمی‌آید**. هر چه شمردنی بود رفت در
`tools/rules_test.sh` و اینجا فقط فهرست شده: گارد مرجع است، نه این متن. و هر چه
واژه‌ی دامنه بود در `CONTEXT.md` است و دوباره اینجا نوشته نمی‌شود.

## از کجا شروع کن

ریشه `~/Projects/tools/utils/zemzeme` است. اول `CONTEXT.md`، بعد
`extras/docs/dev-notes/DEEP-MODULES-2026-08-22.md` و
`extras/docs/dev-notes/SPRING-CLEAN-2026-08-22.md` و `git log`. اولین واحد `todo`
را بردار. از صفر نقشه نکش و نپرس «بعدی چیست».

## کدام فایل کجا

آزمونش: **اگر کد عوض شود و این فایل باید در همان برنچ عوض شود، جایش کنارِ کد است.**
باقی می‌رود `extras/`. نثر درباره‌ی کد آنجاست؛ برنامه‌ای که کد را صدا می‌زند در
`tools/` می‌ماند. شرحش در `extras/README.md`.

## گیت

روی مک، و تنها جایی که «رفتار عوض نشد» اثبات می‌شود:

```bash
bash tools/release.sh
```

روی ویندوز کامپایلر نیست، پس فقط این سه می‌دوند و بقیه `todo` می‌مانند:

```bash
bash tools/rules_test.sh && bash tools/paths_test.sh && bash tools/ledger_test.sh
```

خروجیِ هر کدام را جدا بخوان، نه از پشتِ لوله.

## قاعده‌های گارددار، فقط فهرست

- خط تیره‌ی بلند و نیم‌خط تیره: هیچ. `tools/rules_test.sh` بند ۱
- `python` بی ۳ در دستور: هیچ. بند ۲
- `NSLog`: هیچ، `ZLog` بگذار. بند ۳
- ایموجی در سورس: هیچ. بند ۴
- `@"fa-IR"` فقط کم می‌شود (واحد U12). بند ۵
- فایلِ بالای ۷۰۰ خط، و جمعِ اعلانِ عمومیِ سرآیندها: فقط پایین. `tools/ledger_test.sh`
- مسیرِ نام‌برده در متن باید باشد؛ گذشته `[حذف‌شده]`، آینده `[آینده]`. `tools/paths_test.sh`

## چیزهایی که حدس زدنی نیست

- **ObjC است نه سوئیفت، و انتخاب نیست.** CLT نصب‌شده (`swiftlang-6.2.0.19.9`) با
  ماژول‌های همه‌ی SDK های موجود (`6.2.0.17.14` و قدیمی‌تر) ناسازگار است. `clang`
  سالم است. شرحش سرِ `zemzeme.h`.
- **هیچ وابستگیِ بیرونی ندارد، هیچ.** نه SPM، نه CocoaPods، نه Carthage.
- روی مک فقط `python3` هست.
- `tools/release.sh` از کامیت `a47d1ba` با گلابِ `tools/*_test.sh` تست‌ها را پیدا
  می‌کند. **اسمِ تستِ تازه را دستی اضافه نکن**، فقط فایل را با پسوندِ `_test.sh` بساز.
- `.readable/` و `app/fonts/` و `app/prompts/` و `app/build.sh` دست نمی‌خورند.
  دلیلِ هر کدام در بندِ «خطِ سرخ‌ها»ی سندِ خونه‌تکونی.

## چیزی که این ریپو ندارد و کسی اینجا نمی‌سازد

هر بند یک چیزِ واقعا ردشده یا واقعا برداشته‌شده است:

- **سنجشِ دسترسیِ شبکه** (`SCNetworkReachability`، `NWPathMonitor`، probe). پشتِ
  پروکسی و مسیرِ tun سیستم «آنلاین» می‌گوید در حالی که گوگل در دسترس نیست. **خودِ
  تلاشِ دوباره تنها سنجشِ موجود است.**
- **استثنای per-app برای روشِ نوشتن.** کلیدِ `perApp` خوانده نمی‌شود و
  `tools/insert_test.m` برگشتش را می‌گیرد: شبِ ۱۹ مرداد ۱۴۰۵ متن را در ریموت به
  «aaaa» تبدیل کرد.
- **دفترِ پیچیدگی.** شمردنِ طولِ تابع در ObjC بی پارسر هیوریستیک است، و قرمزِ الکی
  گاردی است که کسی جدی نمی‌گیرد (M7، ردشده با دلیل).
- **ورکسپیسِ جدا.** ۲۲ آگوست ۲۰۲۶، تصمیمِ کاربر. `extras/` بس است.
- **انتظارِ بی‌سقف روی مسیرِ زنده.** گارد ندارد چون گرپش مثبتِ کاذب می‌دهد
  (تایمرِ یک‌بارمصرف هم `DISPATCH_TIME_FOREVER` می‌گیرد). اگر انتظاری اضافه کردی،
  بنویس کدام ساعت سقفش را می‌گذارد.

## آنچه هیچ اسکریپتی نمی‌شمارد

- **عدد بی دستور، ادعاست.** هر سنجه با دستورِ تولیدش و تاریخش نوشته می‌شود، وگرنه
  اندازه‌گیریِ دوم جوابِ سوالِ دیگری می‌دهد.
- **«رفتار عوض نشد» فقط با کامپایل و اجرا روی مک اثبات می‌شود.** روی ویندوز ادعاست،
  و آن واحد `todo` می‌ماند حتی وقتی تشخیص قطعی است.
- **تصمیم را بیرون بکش، خط را جابه‌جا نکن.** تکه کردنِ یک فایلِ بزرگ به سه فایلِ
  متوسط رابط را کوچک نمی‌کند، پخش می‌کند. سنجه‌اش سنجاقِ SURFACE دفتر است، نه شمارِ خط.
- **یک مبدل یعنی درزِ فرضی، دو مبدل یعنی درزِ واقعی.** درزِ تک‌مبدلی فقط واسطه است.
- **تستی که از آن‌طرفِ رابط می‌سنجد، شکلِ ماژول را غلط اعلام می‌کند.** رابط همان سطحِ
  تست است.

قاعده‌ای که وارد این صفحه می‌شود یکی را بیرون می‌کند. سقفِ بایتش بند ۶ است.
