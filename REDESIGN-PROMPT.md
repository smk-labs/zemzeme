# پرامپت بازطراحی ابزار دیکته (برای چت جدید، کپی کن و بفرست)

می‌خوام ابزار دیکته‌ای که توی `~/Projects/hobby/mem/dictate/` دارم رو بازطراحی و کامل کنی. اول این فایل و کل پوشه رو بخون، بعد پلن بده، بعد بساز.

## چی الان وجود داره و کار می‌کنه

- `index.html`: صفحه دیکته با Web Speech API کروم (موتور سروری گوگل، فارسی و انگلیسی، متن زنده کلمه به کلمه). کیفیتش برای فارسی عالیه و همین موتور باید حفظ بشه.
- `serve.py`: سرور پایتونی روی `127.0.0.1:17635` با سه endpoint: استاتیک، `/save` (ذخیره تکه‌های قطعی هر سشن توی `sessions/`)، `/log` (لاگ دیباگ)، `/paste` (پیست خودکار با osascript که فعلا کار نمی‌کنه، احتمالا چون اجازه Accessibility به python داده نشده).
- شورتکات Karabiner توی `~/.config/karabiner/assets/complex_modifications/google-dictation.json`: دابل‌تپ Command راست با ترفند lazy (تپ تنها هیچی نمی‌فرسته که توی Windows App استارت باز نشه). این باید همینطور بمونه.
- باگ پریدن متن خاکستری فیکس شده (salvage در onend)، دکمه تثبیت (Enter با abort و restart)، ویرایش دستی (contenteditable)، کپی فقط موقع Esc یا دکمه.

## فکت‌های تست‌شده (دوباره تست نکن، ازشون استفاده کن)

- شبکه کاربر: sing-box با روتینگ process/domain؛ کروم و google.com از پراکسی (خروجی آلمان) رد می‌شن و سالمه.
- endpoint گفتار گوگل `speech-api/v2/recognize` و حتی استریم `speech-api/full-duplex/v1` با کلید عمومی Chromium (کلید معروف AIzaSyBOti4mM-6x9WDnZIjIeyEU21OpBXqWBgw) از curl همین مک تست شد و جواب زنده داد. یعنی موتور گوگل خارج از کروم هم قابل دسترسیه.
- WKWebView تشخیص گفتار وب نداره؛ یعنی اپ نیتیو نمی‌تونه Web Speech کروم رو embed کنه.

## نتیجه تحقیق کلید Chromium (دو ایجنت، 2026-07-24؛ دوباره تحقیق نکن)

- همین کلید، کلید پیش‌فرض کتابخانه پایتونی SpeechRecognition (میلیون‌ها نصب) هست، تا امروز توی سورس master؛ داکش می‌گه «برای استفاده شخصی، هر لحظه ممکنه گوگل باطلش کنه».
- عدد معروف «۵۰ درخواست در روز» مال کلیدهای شخصی ثبت‌شده از مسیر رسمی Chromium هست، نه این کلید اشتراکی. هیچ گزارش دست‌اولی از throttle شدن این کلید در مصرف متوسط پیدا نشد.
- هر دو endpoint امروز زنده‌ان (تست زنده). سشن‌های full-duplex حدودا تا ۵ دقیقه‌ان، پس معماری باید مثل الان سشن رو بازوصل کنه (الگوی salvage فعلی).
- مسیر رسمی (کلید خودمون) بن‌بسته: فقط برای توسعه Chromium، بدون مسیر پولی.
- ریسک واقعی: قطع یک‌طرفه بدون اطلاع. روند گوگل هم مهاجرت Web Speech به موتور آفلاین SODA هست که فارسی نداره؛ یعنی آینده endpoint شبکه‌ای نامعلومه (فعلا برای زبان‌های بدون SODA مثل فارسی سرپاست).
- **پیامد معماری (الزامی):** موتور رو pluggable بساز. Engine 1: full-duplex با کلید Chromium (اصلی). Engine 2: فال‌بک به همین صفحه کروم فعلی (نگهش دار). Engine 3 (بعدا): آفلاین Qwen3-ASR یا فاین‌تیون فارسی ویسپر. اگه کلید مرد، سوییچ می‌کنیم و محصول زنده می‌مونه.

## مشکل‌های باز که باید حل بشن (به ترتیب اهمیت)

1. **فول‌اسکرین و Spaces**: وقتی کاربر توی اپ فول‌اسکرینه (مثلا Windows App)، باز شدن پنجره کروم دسکتاپ رو سوئیچ می‌کنه. فقط یه پنل نیتیو (NSPanel با canJoinAllSpaces و fullScreenAuxiliary) می‌تونه روی فول‌اسکرین شناور بشه.
2. **پیست خودکار** بعد از Esc کار نمی‌کنه (کپی کار می‌کنه). یا اجازه Accessibility رو درست کن یا در معماری جدید نیتیوش کن.
3. **کلیپ‌بورد**: هر سشن فقط یه ورودی توی کلیپ‌بورد و تاریخچه‌ش بسازه. توی اپ نیتیو می‌شه کپی‌های میانی رو با pasteboard type مخصوص `org.nspasteboard.TransientType` زد که اپ‌های تاریخچه ندیدشون بگیرن.
4. **UI**: طراحی درست‌وحسابی با اسکیل فارسی Vazirmatn، حالت روشن/تاریک، دکمه‌ها با برچسب شورتکات (الان ابتدایی هست، قابل قبوله ولی «طراحی» نیست).

خارج از اسکوپ (تصمیم قطعی، پیشنهادش نده): پانکچوئیشن گفتاری.

## قدم‌های آینده (الان نساز، فقط در معماری جا بذار)

1. **پاس اصلاح متن** (تحقیق شده 2026-07-24، دو لایه):
   - لایه خودکار (حین قطعی شدن متن، آنی و کم‌ریسک): نرمال‌سازی با `piraye` (نیم‌فاصله، ارقام، یکدست‌سازی حروف؛ فعال و به‌روز)، نقطه‌گذاری با مدل `MohammadJRanjbar/parsbert-persian-punctuation` (مخصوص فارسی، مناسب real-time) یا `1-800-BAD-CODE/xlm-roberta_punctuation_fullstop_truecase` (فارسی تاییدشده، خروجی ONNX)، املای محافظه‌کارانه با hunspell و دیکشنری `b00f/lilak`.
   - لایه دکمه‌ای (بازنویسی با LLM لوکال): **پارک شد به تصمیم سید؛ نساز.** فقط لایه خودکار در دستور کاره و پرامپت جدا و کاملش توی `POLISH-PROMPT.md` همین ریپوئه (بعد از بازطراحی اجرا می‌شه). اگه روزی برگشت: کاندیدها Aya Expanse 8B، Qwen3 8B با thinking خاموش، Dorna2 8B روی Ollama با MLX.
   - **هشدار حیاتی (مدرک‌دار):** LLM عمومیِ فاین‌نشده رو برای اصلاح «خودکار» فارسی به کار نبر؛ تنها مطالعه GER فارسی نشون داده Llama-2-7B خام WER رو از ۳۱٪ به ۶۴٪ بدتر کرده (arXiv 2512.17247). LLM فقط توی لایه دکمه‌ای که کاربر خودش می‌بینه و تایید می‌کنه.
   - مرجع معماری: superwhisper همین الگو رو شیپ کرده (پس‌پردازش با Llama لوکال)؛ theJayTea/WritingTools هم برای لایه ادغام سیستمی مک (هاتکی سراسری + Ollama/MLX) ارزش مطالعه داره.
2. **ایده پارک‌شده، نسخه تجاری**: افزونه کروم با Web Speech که هر کاربر از کوتای کروم خودش استفاده می‌کنه (مدل افزونه Voice In). قابل تجاری‌سازیه ولی فعلا نه؛ فقط بدون که کد صفحه وب فعلی ممکنه بعدا هسته اون افزونه بشه، پس تمیز نگهش دار.

## تصمیم معماری که باید بگیری (اول تست، بعد انتخاب)

**گزینه A، اپ نیتیو مک (سوئیفت، منوبار + پنل شناور):** آزادی کامل (فول‌اسکرین، پیست، کلیپ‌بورد، هاتکی بدون Karabiner). موتورش مستقیم به `speech-api/full-duplex/v1` با کلید Chromium وصل می‌شه (استریم PCM/FLAC از میکروفن). تحقیق انجام شده و مسیر سبزه (بخش «نتیجه تحقیق کلید» بالا)؛ فقط یه استرس‌تست عملی کوتاه بکن (دو سه سشن ۵ دقیقه‌ای پشت هم) و موتور رو pluggable نگه دار.

نکته‌های فنی گزینه A (تایید‌شده روی همین مک):
- بیلد: Xcode کامل لازم نیست. Command Line Tools نصبه (Swift 6.2)، با SwiftPM یا swiftc بساز، امضای ad-hoc کافیه (شناسه bundle رو ثابت نگه دار که اجازه‌های TCC با هر بیلد نپرن).
- پنل: NSPanel با `nonactivatingPanel` + `canJoinAllSpaces` + `fullScreenAuxiliary`. چون پنل فوکس اپ هدف رو نمی‌دزده، کرسر کاربر همون‌جا که بود می‌مونه؛ پیست خودکار (کپی، بستن پنل، CGEvent برای Cmd+V) دقیقا سر جای کرسر می‌شینه. اجازه Accessibility یه بار به خود اپ داده می‌شه.
- کلیپ‌بورد: کپی‌های میانی با type اضافه `org.nspasteboard.TransientType` که تاریخچه‌گیرها ردش کنن؛ حتی می‌شه بعد از پیست، کلیپ‌بورد قبلی کاربر رو برگردوند.
- چون پنل فوکس نمی‌گیره، حالت «تایپ زنده مستقیم توی اپ هدف» هم ممکنه (کاراکتر به کاراکتر با CGEvent). به‌عنوان حالت اختیاری در نظر بگیر، پیش‌فرض همون پیست آخر کار.
- هاتکی داخل خود اپ با CGEventTap (دابل‌تپ Command راست رو خود اپ تشخیص بده؛ Karabiner برای این کار بازنشسته می‌شه). push-to-talk (نگه داشتن کلید = ضبط) هم گزینه خوبیه.

## مدل UX اپ نیتیو (سید اینو انتخاب کرده، همینو بساز)

مدل «تسمه‌نقاله»: پنل شناور فقط بافر خاکستریه، متن سفید می‌ره سر کرسر.

- دابل‌تپ: یه نوار باریک شناور کنار محل کار باز می‌شه (فوکس نمی‌گیره، کرسر کاربر همون‌جا می‌مونه).
- حرف که می‌زنه، متن خاکستری (حدس لحظه‌ای) توی نوار دیده می‌شه؛ این بازخورد «دارم شنیده می‌شم» براش خیلی مهمه و باید بمونه.
- هر تیکه که قطعی (سفید) شد، از نوار برداشته می‌شه و همون لحظه سر کرسرِ اپ هدف درج می‌شه. یعنی سند کاربر متن نهایی رو جمع می‌کنه و نوار همیشه فقط چند کلمه آخرِ هنوز خاکستریه.
- ویرایش: چون متن نهایی توی خود سنده، کاربر با کیبورد خودش همون‌جا ادیت می‌کنه؛ پنل هیچ متن نهایی‌ای نگه نمی‌داره که لازم باشه کپی بشه.
- پاز و ادامه پی‌درپی عادیه؛ در سکوت چیزی درج نمی‌شه.
- گارد ضروری: اگه اپ جلویی عوض شد، درج خودکار متوقف بشه (و توی نوار جمع بشه) تا کاربر برگرده؛ یه قفل دستی «درج نکن، فقط جمع کن» هم بذار.
- استراتژی درج per-app: تایپ مستقیم (CGEvent unicode) پیش‌فرض؛ برای Windows App (ریموت) شاید chunk-paste امن‌تر باشه چون فوروارد یونیکد مصنوعی توی RDP باید تست بشه. توی پروتوتایپ هر دو رو بسنج.
- Esc: هرچی مونده تثبیت و درج بشه، کل متن سشن هم یه بار توی کلیپ‌بورد (برای بیمه)، پنل بسته.

## الزام: سوپروایزر موتور (باگ مستند با لاگ واقعی)

لاگ `dictate/log.txt` دو حالت خرابی واقعی رو ثبت کرده: (۱) طوفان ری‌استارت، ده‌ها چرخه start/end در یک ثانیه وقتی سرویس ناسالمه؛ (۲) مرگ ساکت، جایی که start استثنا می‌ده و چون قورت داده می‌شد دیگه هیچ‌وقت وصل نمی‌شد و فقط توقف/شروع دستی درستش می‌کرد. صفحه وب الان سه پادزهر داره که موتور نیتیو هم باید از روز اول داشته باشه: ری‌استارت با backoff نمایی (سقف ۵ ثانیه)، تسلیم بعد از ۸ چرخه بی‌نتیجه با پیام روشن به کاربر، و واچ‌داگ که ۱۲ ثانیه سکوت رویداد رو یعنی سشن مرده و خودش می‌کشدش تا از نو وصل بشه. متن خاکستری معلق هم قبل از هر ری‌استارت باید salvage بشه (الگوش توی index.html هست).
**گزینه B، اسپلیت:** کروم موتور بمونه (صفحه فعلی، مینیمایز روی یه Space)، یه پنل نیتیو کوچیک (یا Hammerspoon webview با canJoinAllSpaces) فقط نمایش زنده متن رو از سرور بگیره (SSE توی serve.py) و کنترل‌ها رو بده. مطمئن‌تره ولی دو تیکه‌ست.
**گزینه C، وضع فعلی + رفع پیست:** کمترین کار، مشکل فول‌اسکرین حل نمی‌شه.

نظر قبلی: اول A رو با استرس‌تست کلید بسنج؛ جواب داد همون رو بساز، نداد برو B. فال‌بک آفلاین (بعدا، نه الان): Qwen3-ASR 1.7B یا فاین‌تیون فارسی ویسپر `vhdm/whisper-large-fa-v1`.

## قواعد کار (از دستورالعمل کلی من)

- وابستگی جدید فقط با توجیه جدی؛ ترجیح با کد خودمون. تخمین‌ها به LoC.
- UI فارسی ساده و بی‌ژارگون، بدون ایموجی، RTL درست، اعداد فارسی.
- تهش: تست واقعی (باز کردن، دیکته، Esc)، لاگ چک، و مستندسازی کوتاه توی README همون پوشه.

## معیار پذیرش

دابل‌تپ Command راست توی یه اپ فول‌اسکرین: ورودی دیکته بدون سوئیچ دسکتاپ بیاد بالا، فارسی زنده بنویسه، Esc کل متن رو یه بار توی کلیپ‌بورد بذاره و (اگه معماری اجازه داد) توی همون اپ پیست کنه، و هیچ متن خاکستری‌ای هیچ‌وقت گم نشه.
