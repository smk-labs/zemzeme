# واژه‌نامه دامنه زمزمه

زبان مشترک این ریپو، برای آدم و برای ایجنت. پیش از هر طراحی و هر بازبینی
این را بخوان: اسمی که اینجا هست همان اسمی است که در کد و در گفت‌وگو به کار
می‌رود، و اسم تازه فقط وقتی ساخته می‌شود که هیچ‌کدام از این‌ها کار را نگویند.

زبان این ریپو. هر مدخل: نام فارسی، نام در کد، و اینکه چه تصمیمی می‌گیرد یا چه چیزی را نگه می‌دارد.
منبع: بند «اجزا»ی README.md، سرآیند app/Sources-objc/zemzeme.h، و extras/docs/dev-notes/queue-plan.md.
چیزی از بیرون این سه اضافه نشده. شماره خط عمدا نیست: جابه‌جا می‌شود و این فایل
را بی‌صدا غلط می‌کند.

هر اسمی که اینجا با `backtick` آمده، سنجیده شده که در app/Sources-objc/ وجود دارد.

## صدا و برش

**تکه** — واحد کار. یک بازه صدا که یک متن می‌گیرد و مستقل از بقیه است. همین استقلال است که عوض کردن زبان وسط سشن را تقریبا مجانی می‌کند.

**برش‌زن** — `ZSegFind` در `seg.m`. تنها بخشی که باید درست باشد. در پنجره ۴ تا ۱۲ ثانیه هر مکث دست‌کم ۲۰۰ میلی‌ثانیه‌ای را پیدا می‌کند، با طول و عمق مکث امتیاز می‌دهد، و بالاترین امتیاز را می‌برد نه نزدیک‌ترین به هدف ۷ ثانیه. وسط مکث می‌برد نه لبه‌اش. تابع خالص است و هیچ حالتی نگه نمی‌دارد.

**برش تحمیلی** — `degraded` در `ZSegCut`. اگر در کل پنجره هیچ مکثی نبود، آرام‌ترین فریم ثانیه آخر بریده می‌شود و صریحا در لاگ گزارش می‌شود. هیچ‌وقت بی‌صدا سر تایمر بریده نمی‌شود.

**مکث** — دوره‌ای که بلندی زیر `kZSegQuietMs` و `kZSegRMS` بماند. جواب سوال «اینجا جای بریدن است؟».

**حرف داشتن** — `ZSegHasVoice` با `kZVoiceRMS`. جواب سوال «این تکه اصلا صدایی دارد؟». انرژی می‌سنجد نه حرف، و آستانه‌اش عمدا کوچک است تا پچ‌پچ رد شود. در تردید می‌فرستد، چون انداختن تکه پچ‌پچ گران‌تر از یک رفت‌وبرگشت هدررفته است.

**ته‌مانده** — `tail` در `ZSegCut`. صدا تمام شد و هرچه مانده همین است، هر چقدر هم کوتاه.

**خط لوله** — `ZPipe` در `pipe.m`. صدا می‌گیرد و تکه تحویل می‌دهد. سرهم کردن متن کل الگوریتم است: تکه‌ها با یک فاصله به هم می‌چسبند. نه هم‌پوشانی، نه جوش، نه جست‌وجوی درز.

**موتور** — `engine.m`. میکروفن، ضبط روی دیسک، و خط لوله. و بس. در حین حرف زدن هیچ متنی نمی‌دهد، فقط بلندی صدا برای نشان.

**بافر بک‌لاگ** — `kZBacklogSec` (۶۰ ثانیه). چقدر صدای خام می‌تواند روی شبکه ضعیف معطل بماند پیش از اینکه قدیمی‌ترینش دور ریخته شود.

## صف و تحویل

**صف** — `ZQueue` در `queue.m`. واحد حقیقت را نگه می‌دارد و تصمیم می‌گیرد چه تکه‌ای دوباره برود. یک کارگر، یک درخواست در پرواز، یک ساعت عقب‌نشینی برای کل صف.

**جا** — `ZSlot`. واحد حقیقت. جای ساختاری یک تکه در متن، با حالت و متن و زبان و افست خودش. متن هر لحظه از روی جاها ساخته می‌شود، پس هیچ جراحی رشته‌ای لازم نیست.

**سه حالت** — `ZSlotWaiting` (هنوز جواب نگرفته)، `ZSlotDone` (متن دارد)، `ZSlotSilent` (سرور گوش کرد و حرفی نبود، سهمش در متن هیچ است).

**پایان سالم** — `ZCloseWasClean`. جداکننده «حرفی نبود» از «جواب نگرفتیم». بسته شدن تمیز با متن خالی یعنی تکه تمام است؛ هر دلیل دیگری یعنی باید دوباره رفت. پیش از این هر دو یک شکل داشتند و ۲۰۲۶-۰۸-۱۹ یک نفس ۱٫۴ ثانیه‌ای ۱۷۴۱ نویسه را گروگان گرفت.

**عقب‌نشینی** — `ZBackoffDelay`. پله‌های ۱، ۲، ۴، ۸، ۱۵، ۳۰ ثانیه، مشترک برای کل صف. خرابی مال شبکه است، پس ده تکه در انتظار نباید ده برابر تلاش بی‌فایده بشوند.

**نظر دوم** — `secondOpinionUsed`. یک تلاش فوری اضافه، یک بار در عمر هر تکه، پیش از باور کردن هر کدام از دو حکم.

**دور اول** — `waitForFirstPass` با سقف `kZQueueFirstPassCeilingSec` (۹۰ ثانیه). سر پایان فقط تا اینجا صبر می‌شود، نه تا خالی شدن صف. انتظار بی‌سقف در این اپ ممنوع است.

**متن نشسته** — `settledTextFrom:`. متن تا اولین جای نرسیده و نه یک کلمه بیشتر. تنها متنی که حق دارد سر کرسر برود. پنل و کلیپ‌بورد کل `text` را می‌گیرند.

**دفترچه** — `queue.json`، مسیرش از `ZQueueManifestIn`. سر هر تغییر حال نوشته و همان لحظه که چیزی در انتظار نماند پاک می‌شود. تکه صدای خودش را نگه نمی‌دارد: یک افست فریم و یک تعداد فریم، و صدا همان `audio.flac`.

**برداشتن سر لانچ** — `ZResumePendingQueues` و `ZFinishResumedSession`. هر سشنی که دفترچه دارد بی رابط و بی کلید تمام می‌شود و متنش سر جای خودش در تاریخچه می‌نشیند.

**تاریخچه متن‌ها** — `history.jsonl`. هر متن تحویل‌شده همان لحظه تحویل، یک رکورد در یک خط. شصت روز می‌ماند و برای ماندنش لازم نیست کلیپ‌بورد یا درج درست کار کرده باشند.

## رابط کاربر

**پنل** — `NSPanel` بدون گرفتن فوکس، روی همه Space ها. با آمدنش فوکوس نمی‌گیرد، فقط با کلیک روی متن.

**دو حالت** — «جمع در پنل» و «کنار کرسر». `Command راست + E` بینشان می‌چرخد.

**نقطه** — نشانگر حالت کنار کرسر. بی‌پنل، فقط یک نقطه.

**نشان** — `mark.m`. دو نشان انتظار که عمدا شبیه کار خودشان‌اند: میله‌های نارنجی صدا یعنی صدا دارد متن می‌شود، جرقه‌های آبی یعنی پاس هوش مصنوعی روی متن.

**کارت میان‌برها** — یکی از سه جایی که `ZStopHint` نوشته می‌شود. تک‌تپ Command راست یعنی پایان.

**پیش‌نمایش** — `preview.m`، آزمایشی و پیش‌فرض خاموش. متن خاکستری هم‌گام با حرف زدن، از یک سشن جدا و نمایشی. یک قاعده در کد: خروجی این استریم فقط به دم خاکستری پنل می‌رود؛ نه وارد متن سشن، نه درج، نه کپی، نه روی دیسک.

**پاس هوش مصنوعی** — اختیاری، فقط روی متن. فرمتینگ و اصلاح واژه صد در صد غلط. تنها تور ایمنی‌اش: خروجی کوتاه‌تر از ۷۰٪ ورودی رد می‌شود.

**پاس دوم انگلیسی** — `extra` روی `ZSlot` و `ZPipe`. همان صدا هم‌زمان با `en-US`. متنش هیچ‌وقت تحویل کاربر نمی‌شود و فقط کانتکست پاس هوش مصنوعی است، پس نه در شمار «در راه» می‌آید و نه سر قطعی دوباره فرستاده می‌شود.

**رونویسی فایل** — `Command راست + F`. همان برش‌زن و همان رونویس تکه؛ تفاوت فقط زمان‌بندی است: زنده یکی در هر لحظه، فایل چند تا با هم.

**نقطه رایگان** — تاخیر مسیر سالم که کاربر منتظرش است. یکی از چیزهایی که دست‌نخورده می‌ماند.

**لال** — سشنی که حرف داشت ولی متنش خالی برگشت. یک بار دیگر امتحان می‌شود.

**دفتر** — `tools/ledger_test.sh`. اندازه را می‌گیرد.

**تست طلایی** — تست جدولی برش‌زن روی ضبط‌های واقعی. با `kZSegQuietMs` و شاخص تطبیق سنجیده می‌شود.

## اسم‌های دوکاره

این چهار اسم در کد دو کار می‌کنند و همین‌جا علامت خورده‌اند.

**سشن** — دو چیز. یک: سشن دیکته کاربر، با پوشه، `sid`، `text.txt`، `audio.flac` و ردیف تاریخچه. دو: سشن استریم گوگل، که هر تکه یکی تازه‌اش را می‌گیرد و می‌میرد. «سقف پنج دقیقه» مال اولی است و «یک تکه، یک سشن» مال دومی.

**تکه** — دو چیز. یک: برش برش‌زن، واحد حقیقت صف. دو: بلوک خام ورودی میکروفن، همان تکه‌های ۳۲۰۰ بایتی که در `queue-plan.md` باعث باگ ضبط شدند. هم‌اندازه نیستند و هم‌عمر نیستند.

**پاس** — سه چیز. پاس دوم انگلیسی (رونویسی دوباره همان صدا)، پاس هوش مصنوعی (فقط روی متن)، و پاس تمیزکاری (که سر لانچ عمدا اجرا نمی‌شود چون خواندن کلید می‌تواند پنجره رمز بالا بیاورد).

**درج** — دو چیز. یک: تحویل متن سر کرسر در حالت کنار کرسر. دو: «درج مستقیم»، یکی از دو روش نوشتن در برابر «ذخیره در کلیپ‌بورد».

**بلندی صدا** — دو آستانه که هر دو RMS می‌سنجند ولی به دو سوال جواب می‌دهند: `kZSegRMS` (۰٫۰۲) یعنی «اینجا مکث است؟» و `kZVoiceRMS` (۰٫۰۰۵) یعنی «این تکه صدایی دارد؟». یک عدد برای هر دو سوال غلط بود و روی ضبط ۰۴ تطبیق را از ۷۹٪ به ۱۴٪ انداخت.

**دفتر و دفترچه** — دو چیز نزدیک‌نام. دفتر اندازه فایل‌ها را می‌گیرد؛ دفترچه حال تکه‌های در انتظار یک سشن را نگه می‌دارد.
