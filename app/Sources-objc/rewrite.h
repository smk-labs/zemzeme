// لایه‌ی بازنویسی: یک متن که جای مجموعه‌ای از جاهای صف را گرفته.
//
// دو نویسنده دارد و همین دو تا بودن است که این درز را واقعی می‌کند: پاس هوش مصنوعی،
// و خودِ کاربر که متن را در پنل ویرایش می‌کند. تا دیروز فقط اولی لایه بود و دومی
// جایگزینِ کلِ متن، و همان تفاوت باگِ C1 را ساخت.
//
// چرا سرآیندِ خودش و نه zemzeme.h: مصرف‌کننده‌اش دو تاست، app/Sources-objc/session.m
// و تستِ طلایی. بردنش به سرآیندِ مشترک یعنی هر ۲۶ فایل این دو تابع را ببینند، و
// رابطِ این ماژول همان سطحِ تستش است.
#import <Foundation/Foundation.h>

@class ZSlot;

// متنِ یکتای این لحظه: `rewrite` (اگر باشد)، و پشتش هر جایی که `covers` جایش را
// نگرفته، به ترتیبِ خودش و با یک فاصله.
//
// `covers` **مجموعه** است نه مرزِ عددی، و همین نکته‌ی اصلی است: متنِ زنده افزودنی
// نیست. جای وسطِ صف می‌تواند هنوز در انتظار باشد و وقتی برسد متن از وسط بزرگ شود.
// مرزِ عددی («هر جایی که شماره‌اش از این بزرگ‌تر است دُم است») آن جا را برای همیشه
// می‌انداخت، یعنی دقیقا همان حالتی که این اپ برای زنده ماندن از آن ساخته شده.
//
// `settled` یعنی تا اولین جای نرسیده و نه یک کلمه بیشتر: تنها متنی که حق دارد سر
// کرسر برود. پنل و کلیپ‌بورد و تاریخچه کلِ متن را می‌گیرند.
NSString *ZRewriteText(NSString *rewrite, NSIndexSet *covers,
                       NSArray<ZSlot *> *slots, BOOL settled);

// و مجموعه‌ای که آن متن جایش را گرفته: `covers` به‌اضافه‌ی هر جایی که سهمش همین حالا
// قطعی شده. لایه‌ی بعدی روی همین سوار می‌شود، چه پاسِ بعدی باشد چه ویرایشِ کاربر
// روی همان متنی که جلوی چشمش است.
NSIndexSet *ZRewriteCovers(NSIndexSet *covers, NSArray<ZSlot *> *slots);
