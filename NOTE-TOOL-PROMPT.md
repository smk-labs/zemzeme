# پرامپت ۱ از ۳: ابزار چهارم زمزمه، «یادداشت» بی‌استریم (برای چت نو)

می‌خوام یک حالت چهارم به زمزمه (همین ریپو) اضافه کنی: **بی‌هیچ استریمی**. فقط صدا را ضبط می‌کند. وقتی سشن تمام شد، کل صدا را یک‌جا به LLM می‌دهد و یک متن تمیز و کامل برمی‌گرداند. همین.

اول کل ریپو را بخوان، بعد پلن کوتاه بده، بعد بساز و تا تست و پوش برو. پوش مستقیم به main، بدون برنچ و بدون PR.

## این پرامپت صاحبِ موتور مشترک است

دو کار دیگر بعد از تو روی همین موتور سوار می‌شوند و پرامپت خودشان را دارند: `FINAL-PASS-PROMPT.md` (همین پاس روی سه حالت فعلی و پنل رونویسی فایل) و `PROMPT-ENHANCE-PROMPT.md` (تبدیل متن آماده به یک پرامپت حرفه‌ای). **تو اول اجرا می‌شوی و زیرساخت را می‌سازی**، آن‌ها فقط مصرف‌کننده‌اند.

پس این‌ها را طوری بساز که از بیرون قابل استفاده باشند، نه چسبیده به حالت یادداشت:

- کلاس `ZFinalPass` با یک ورودی روشن: «این فایل صدا و این تنظیمات را بگیر، متن نهایی بده».
- نوشتن صدای سشن روی دیسک، به شکلی که بعدا برای هر سشنی (نه فقط یادداشت) قابل روشن کردن باشد.
- دروازه‌ی کامل بودن، به شکل تابع مستقل و قابل تست.
- حالت `mechanical` روی `polish.py`.
- پرامپت‌ها به شکل فایل در `Contents/Resources`.
- خواندن کلید از Keychain.

## وضعیت ریپو (دوباره کشفش نکن)

- اپ نیتیو منوبار، ObjC در `app/Sources-objc/` (۲۰ فایل، ~۱۰٫۲ هزار خط)، بیلد با `bash app/build.sh`. سوئیفت روی این مک بیلد نمی‌شود؛ به `app/swift-port/` دست نزن.
- **حالت‌ها**: `ZMode` در `zemzeme.h:46` با `ZModeLive=0`، `ZModeCollect=1`، `ZModeCursor=2`. روی دیسک زیر کلید قدیمی `collect` به شکل عدد ذخیره می‌شود (`core.m:182`). حالت تازه `ZModeNote=3` می‌شود و همین مکانیزم بی‌مهاجرت جوابش را می‌دهد.
- **میکروفن**: `ZMic` در `zemzeme.h:266` و `audio.m`. همین حالا PCM خام `s16le` مونو ۱۶ کیلوهرتز در تکه‌های ~۱۰۰ میلی‌ثانیه می‌دهد و کال‌بک روی نخ صداست. برای ضبط چیز تازه‌ای لازم نیست.
- **انکودر FLAC**: `flac.m` (~۱۹۱ خط) از قبل هست و مسیر آپلود زنده از آن استفاده می‌کند. فایل ضبط را با همین FLAC بنویس، نه WAV: حجم را ~نصف می‌کند و جمینای هم FLAC را مستقیم می‌گیرد.
- **پاس قاعده‌ای فعلی**: `ZPolish` در `zemzeme.h:285` و `polish.m`، پل به دیمن پایتون `app/py/polish.py` روی `127.0.0.1:17636` با سقف ۳۰۰ میلی‌ثانیه.
- **پنل**: `ui.m` (کلاس `ZSession` و پنل شناور). منو در `app.m` (تاگل `polishEnabled` در خط ۳۳۴ و ۴۷۷، الگوی `menuNeedsUpdate`). کارت میان‌برها در `cheatsheet.m`. دفتر متن در `transcript.m`.
- داده در `~/Library/Application Support/Zemzeme`: `sessions/`، `transcripts/`، `app.log`، `polish.log`، `py/`.

## اندازه‌گیری‌های انجام‌شده (دوباره تحقیق نکن)

آزمایشگاه مستقل در `~/Projects/tools/utils/zemzeme-lab` است: `lab.py` (پایتون، فقط stdlib) پنج مسیر را روی یک ویس واقعی فارسی سنجیده. **اول آن را بخوان**؛ پیاده‌سازی مرجعِ کاری همان است و `prompts/` اش دو پرامپت تیون‌شده دارد.

روی ۳۶۷ ثانیه فارسی واقعی (`voice.ogg`) با `gemini-3.6-flash`:

| مسیر | محتوای برگشته | زمان با `thinking_level: minimal` |
| --- | --- | --- |
| STT فعلی زمزمه، بعد پاس متنی | **۰ از ۱۱** | ۱۴۰ ثانیه رونویسی + ۲۸ |
| صدا در یک تماس تا متن نهایی | ۱۰ از ۱۱ | ۲۲ ثانیه، ولی **خروجی خراب شد** |
| صدا ← رونویسی مو‌به‌مو ← پاس متنی | ۱۰ تا ۱۱ از ۱۱ | **۲۱ ثانیه** |

- متن خام STT فعلی فقط **۷۷٪** کلمه‌های گفته‌شده را داشت. یازده تکه محتوا (یک جمله‌ی کامل، اسم شرکت، «اپ اسنپ»، «هوش مصنوعی» و بقیه) اصلا در آن نبود، پس هیچ پاس متنی نمی‌توانست برشان گرداند. **این دلیلِ کل تصمیم است: در این حالت هیچ STT ای در مسیر نیست.**
- هزینه: ~۲۵ توکن برای هر ثانیه صدا. کل مسیر دومرحله‌ای برای ۶ دقیقه صدا ~۱۵ هزار توکن و ۲ درخواست، یعنی روی سهم مجانی صفر و روی تیر پولی ~۴ سنت.

## معماری

مسیر دومرحله‌ای، هر دو مرحله با `thinking_level: minimal`:

```
میکروفن ← فایل FLAC روی دیسک
   (پایان سشن)
   ← تماس ۱: صدا ← رونویسی مو‌به‌مو
   ← تماس ۲: آن متن ← پاس تمیزکاری
   ← دروازه‌ی کامل بودن
   ← پاس مکانیکی قاعده‌ای (نیم‌فاصله و ارقام)
   ← ادیتور پنل + کلیپ‌بورد
```

کلاس `ZFinalPass` جدا از `ZPolish` باشد. موتور زنده نباید به آن وابسته شود.

**تماس با جمینای**

```
POST https://generativelanguage.googleapis.com/v1beta/interactions
Headers: x-goog-api-key: <کلید>, Content-Type: application/json
{
  "model": "gemini-3.6-flash",
  "system_instruction": "<متن پرامپت>",
  "input": [ {"type":"text","text":"..."},
             {"type":"audio","uri":"<از Files API>","mime_type":"audio/flac"} ],
  "generation_config": { "thinking_level": "minimal" }
}
```

- مدل را **پین کن** روی `gemini-3.6-flash`. `gemini-flash-latest` هم کار می‌کند و همین حالا به همان می‌رسد، ولی alias بی‌خبر جابه‌جا می‌شود و خروجی را غیرقابل مقایسه می‌کند.
- مقادیر مجاز `thinking_level`: `minimal`، `low`، `high`. `none` و `off` خطای ۴۰۰ می‌دهند. `minimal` صفر توکن فکر می‌دهد و همان چیزی است که می‌خواهیم.
- **پاسخ**: کلیدهای سطح بالا `id, status, usage, created, updated, service_tier, steps, object, model`. متن داخل `steps` است. شکل دقیق مستند نیست، پس مثل `lab.py` بگرد: در کل JSON هر کلید `text` را جمع کن و هر دیکشنری با `thought: true` را رد کن. مصرف در `usage`: `total_input_tokens`، `total_output_tokens`، `total_thought_tokens`، `total_tokens`. پاسخ خام هر تماس ناموفق را روی دیسک بنویس.
- **فرستادن صدا**: زیر ۱۵ مگابایت درون‌خطی با base64؛ بالاتر با Files API (سه مرحله‌ی resumable: `POST /upload/v1beta/files` با هدرهای `X-Goog-Upload-*`، آدرس آپلود از هدر `X-Goog-Upload-URL`، بعد ارجاع با `uri`). فایل تا ۴۸ ساعت زنده می‌ماند و سقف صدا ۹٫۵ ساعت است، پس **هیچ چرخش و جوش درزی در این مسیر لازم نیست**. کل ماشین‌آلات چرخش مال موتور زنده است و این حالت با آن کاری ندارد.
- **کلید** در Keychain، سرویس `zemzeme-gemini`. نه در plist، نه در `.env`، نه در ریپو. کلید نبود، حالت یادداشت خودش را با پیام روشن غیرفعال نشان بدهد.

**پرامپت‌ها فایل‌اند، نه رشته‌ی هاردکد.** دو فایل تیون‌شده از قبل در `app/prompts/` هستند (`verbatim.md` و `polish.md`، عینا همان‌هایی که در آزمایشگاه سنجیده شدند). کاری که مانده: در `app/build.sh` آن‌ها را به `Contents/Resources` کپی کن و در کد از همان‌جا بخوان، تا ویرایش پرامپت بیلد تازه نخواهد. از نو ننویسشان؛ هر جمله‌شان جواب یک باگ اندازه‌گیری‌شده است.

**پاس مکانیکی بعد از LLM.** به `polish.py` یک حالت `mechanical` اضافه کن (پارامتر روی `/polish` یا اندپوینت `/format`) که فقط نیم‌فاصله‌ی تاییدشده با دیکشنری، ارقام فارسی، یکدست‌سازی حروف عربی و فاصله‌ی علائم را بزند. **املای hunspell و مدل نقطه‌گذاری در این مسیر اجرا نشوند**: کور و بی‌کانتکست‌اند و LLM هر دو را بهتر انجام داده. LLM ها با ZWNJ بی‌دقت‌اند، پس همین تکه‌ی مکانیکی می‌ارزد.

## قواعد سخت

- **مسیر زنده دست‌نخورده می‌ماند.** بودجه‌ی ۳۰۰ میلی‌ثانیه‌ی `ZPolish` و قاعده‌ی «هیچ LLM مولدی در پاس زنده نیست» سر جایشان‌اند. سه حالت فعلی در این کار هیچ تغییری نمی‌کنند؛ آن‌ها کار پرامپت بعدی‌اند.
- **دو تماس، نه یک تماس.** اندازه‌گیری شد: تماس یکجای «صدا تا متن نهایی» با `minimal` خروجی درهم و دوباره‌نویسی‌شده داد (۲۰۷۲ کلمه به جای ۹۶۵). دو مرحله هم سریع‌تر است هم سالم.
- **هیچ چیز گم نمی‌شود.** فایل صدا، متن مو‌به‌مو و متن نهایی هر سه ذخیره می‌شوند. هر سه با یک میان‌بر قابل چرخش باشند تا کاربر بتواند مقایسه کند.
- **مسیر خروج اپ منتظر پاس نمی‌ماند.** پاس را می‌بازیم، متن را نه.
- سشن `en-US` هم از همین مسیر رد می‌شود (مدل چندزبانه است)، ولی پاس مکانیکی فارسی روی آن اجرا نشود.

## دروازه‌ی کامل بودن

بازنویسی مولد بی‌صدا محتوا می‌خورد، پس قبل از قبول کردن خروجی:

- **عدد و لاتین، تحمل صفر.** هر رقم و توکن لاتینِ متنِ مو‌به‌مو باید در متن نهایی باشد. نبود، خروجی رد می‌شود.
- **پوشش واژه‌های محتوایی** بین متن مو‌به‌مو و متن نهایی. زیر آستانه (~۹۵٪ با فهرست فیلر) هشدار بده.
- رد شد؟ یک بار با پرامپت سخت‌گیرتر دوباره بفرست؛ باز رد شد، متن مو‌به‌مو بنشیند. قاعده‌ی خود ریپو: در تردید، خام.
- تابع `measure()` در `lab.py` را پورت کن، **با هر دو باگی که در آن درست شده**: نیم‌فاصله باید به فاصله تبدیل شود نه حذف، و تطبیق باید بی‌فاصله هم امتحان شود. بدون این دو، هر نیم‌فاصله‌ی درست «واژه‌ی جاافتاده» شمرده می‌شود و دروازه الکی می‌بندد.

## تنظیمات و رابط

- حالت چهارم در چرخه‌ی `E` اضافه شود و در کارت میان‌برهای `cheatsheet.m` هم بیاید.
- تاگل ماندگار «پاس نهایی با هوش مصنوعی» در منو (پیش‌فرض خاموش تا کلید ست شود).
- **پنل باید زنده به نظر برسد.** در حین ضبط: زمان سپری‌شده و نشان شنیدن. بعد از `Esc`: نشان پیشرفت تا جواب برسد (~۲۰ ثانیه برای ۶ دقیقه صدا). پنل مرده بدترین حالت این فیچر است، چون در این حالت هیچ متن خاکستری‌ای نیست که کاربر را سرگرم کند.
- شکل خروجی با پرامپت تعیین می‌شود نه با تنظیم: بولت فقط وقتی که خودِ گفتار شمرده باشد، وگرنه پاراگراف. یک تاگل «همیشه ساده» برای کسی که بولت نمی‌خواهد کافی است.
- صدای سیستمی و پیام کوتاه خط وضعیت مثل بقیه‌ی کارها (هر جا `flash` هست، `ZPlay` هم هست). آیکون SVG یا سیستمی، بدون ایموجی.

## تله‌هایی که قبلا خوردیم

- **متن کمکی را «مرجع» صدا نزن.** در آزمایش، جمله‌ی «متن خام مرجع کامل بودن است» باعث شد مدل فقط ۳ از ۱۱ تکه‌ی گم‌شده را برگرداند، یعنی بدتر از حالتی که هیچ متنی نداشت. متنِ ناقصی که مرجع معرفی شود سقف می‌شود نه لنگر.
- **رسمی‌سازی لحن.** قاعده‌ی کلی «لحن را حفظ کن» کافی نبود؛ مدل «می‌تونم» را «می‌توانم» می‌کرد. با شش مثال مشخص در پرامپت به صفر رسید. پرامپت‌های آزمایشگاه همین اصلاح را دارند.
- **مقدمه‌ی خودساخته.** با `minimal`، مرحله‌ی رونویسی یک جمله از خودش اضافه کرد («متن پیاده‌شده به شرح زیر است»). هم پرامپت را سخت کن هم یک لایه‌ی ارزان: اگر خط اول متن نبود، بیندازش.
- **قطعی شبکه.** یک بار وسط کار اتصال افتاد و کل اجرا مرد. هر تماس باید تلاش دوباره داشته باشد.

## تست و تحویل

- ست طلایی از همان آزمایشگاه: `voice.ogg` و خروجی‌های مرجع در `out/` هستند. خروجی اپ باید با آن‌ها قابل مقایسه باشد.
- تست خودکار دروازه: متنی که عدد را انداخته باید رد شود، متنی که فقط فیلر را برداشته باید قبول شود، و پاس روی متن پاس‌خورده باید ایدمپوتنت باشد.
- ویس دوم لازم است که واقعا چیزی بشمارد، تا قاعده‌ی بولت در جهت مثبت هم آزموده شود. ویس فعلی روایی بود.
- بیلد، اجرای واقعی، چک `app.log`، بخش تازه در README (چه می‌کند، چه چیزی عمدا نمی‌کند، تاگل و میان‌بر کجاست، عددهای اندازه‌گیری‌شده)، کامیت و پوش به main.
- **کار جانبی لازم**: `.env` حالا در `.gitignore` هست، ولی کلید هنوز در همان فایل است. کلید را به Keychain منتقل کن (سرویس `zemzeme-gemini`) و بعد `.env` را پاک کن. کد نباید هیچ‌وقت `.env` را بخواند.

## معیار پذیرش

در حالت یادداشت یک متن سه دقیقه‌ای فارسی گفته شود. هیچ استریمی نرود، هیچ متنی وسط کار جایی درج نشود. حدود ۱۵ ثانیه بعد از `Esc`، متنی تمیز و کامل در ادیتور بنشیند: بی‌فیلر، با نقطه‌گذاری و نیم‌فاصله‌ی درست، با حفظ لحن محاوره، بی‌هیچ عدد یا اسم گم‌شده، و بولت فقط اگر واقعا فهرست گفته شده باشد. کلید نباشد یا شبکه قطع باشد، هیچ متنی گم نشود. سه حالت قدیمی مو‌به‌مو مثل امروز کار کنند.
