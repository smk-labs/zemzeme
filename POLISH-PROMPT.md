# پرامپت ساخت پاس ویرایش خودکار فارسی (برای چت جدید، بعد از بازطراحی)

می‌خوام به ابزار دیکته harf (همین ریپو) یه «پاس ویرایش خودکار» اضافه کنی: هر تیکه متن که قطعی می‌شه (خاکستری به سفید)، قبل از نشستن سر جاش، آنی و بی‌سروصدا تمیز بشه. اول کل ریپو رو بخون و وضعیت فعلی رو بفهم، بعد پلن کوتاه بده، بعد بساز و تا تست و پوش برو.

## زمینه

- این ریپو یه ابزار دیکته فارسیه با موتور گوگل. بسته به اینکه پرامپت بازطراحی (`REDESIGN-PROMPT.md`) اجرا شده یا نه، یا اپ نیتیو مک هست یا صفحه وب (`index.html` + `serve.py`). **اول چک کن کدومه** و پاس رو به همون وصل کن: توی اپ نیتیو به مسیر تسمه‌نقاله (قبل از درج سر کرسر)، توی صفحه وب به لحظه قطعی شدن متن (تابع `appendFinal`) از طریق یه endpoint جدید مثل `/polish` توی `serve.py`.
- کاربر فارسی دیکته می‌کنه، گاهی انگلیسی. متن انگلیسی دست‌نخورده رد بشه.
- نمونه واقعی دیکته‌های کاربر (با خطاهای واقعی STT) توی `sessions/*.txt` هست؛ طلاست، برای تست ازش استفاده کن.

## چی می‌سازی: فقط اصلاح‌های قطعی و بی‌ریسک

سه جزء، هر سه لوکال و رایگان (تحقیق شده 2026-07-24، دوباره تحقیق نکن):

1. **نرمال‌سازی** با `piraye` (pip، فعال و به‌روز): نیم‌فاصله، یکدست‌سازی حروف عربی/فارسی، ارقام فارسی، فاصله‌گذاری علائم.
2. **نقطه‌گذاری** با مدل `MohammadJRanjbar/parsbert-persian-punctuation` (مخصوص فارسی، real-time). اگه به هر دلیل جواب نداد، فال‌بک: `1-800-BAD-CODE/xlm-roberta_punctuation_fullstop_truecase` (فارسی تاییدشده، ONNX داره). مدل یه بار دانلود و کش می‌شه؛ بعدش کاملا آفلاین.
3. **املای محافظه‌کارانه** با hunspell و دیکشنری `b00f/lilak`: فقط وقتی کلمه غلطه و دقیقا یه پیشنهاد با فاصله ویرایشی کم وجود داره اصلاح کن؛ کوچک‌ترین ابهام یعنی دست نزن.

## قواعد سخت (نقض هیچ‌کدوم قابل قبول نیست)

- **هیچ LLM مولدی توی این پاس نیست.** مدرک: تنها مطالعه اصلاح STT فارسی (arXiv 2512.17247) نشون داده LLM خام خطا رو تقریبا دو برابر می‌کنه. این پاس فقط قواعد و مدل‌های طبقه‌بندی کوچیکه.
- **معنا هیچ‌وقت عوض نمی‌شه.** لیست سفید عملیات: فاصله و نیم‌فاصله، یکدست‌سازی نویسه، ارقام، درج علائم نقطه‌گذاری، اصلاح املایی تک‌پیشنهادی. هیچ جابجایی، حذف یا بازنویسی کلمه. اصلاح خطاهای شنیداری (مثل «اکاسی» به جای «عکاسی») خارج از اسکوپه چون بدون درک معنا ریسکیه.
- **بودجه تاخیر:** هدف زیر ۱۵۰ میلی‌ثانیه برای هر تیکه، سقف سخت ۳۰۰. دیر شد؟ همون متن خام رد بشه؛ تسمه‌نقاله هیچ‌وقت منتظر پاس نمی‌مونه. مدل‌ها یه بار موقع استارت لود و گرم نگه داشته بشن.
- **ایدمپوتنت:** پاس روی متنِ پاس‌خورده باید no-op باشه.
- **به متن ویرایش‌شده کاربر دست نزن:** فقط تیکه تازه قطعی‌شده پاس می‌خوره، هیچ‌وقت متن قبلی دوباره پردازش نمی‌شه.
- **تاگل:** یه سوییچ روشن/خاموش ماندگار (پیش‌فرض روشن). خاموش یعنی رفتار خام فعلی.
- وابستگی‌ها حداقلی و توجیه‌شده: piraye، hunspell (یا باینـدینگ pyhunspell)، و یه رانتایم اینفرنس (ترجیحا onnxruntime به جای torch کامل اگه شدنیه). بیشتر از این نه.

## تست و تحویل

- یه ست طلایی بساز: حدود ۳۰ جفت «قبل/بعد» از دل `sessions/*.txt` واقعی + چند کیس مصنوعی (نیم‌فاصله «می‌شود»، ارقام، جمله بی‌نقطه، کلمه با یه غلط تایپی روشن، متن انگلیسی که باید دست‌نخورده بمونه، متن قبلا پاس‌خورده که باید no-op باشه). تست‌ها با یه دستور اجرا بشن.
- تاخیر رو اندازه بگیر و توی خروجی تست چاپ کن (میانگین و بدترین حالت روی همین مک).
- لاگ محلی قبل/بعد برای هر تیکه (فایل، بدون هیچ ارسال بیرونی) که اگه اصلاح بدی دیده شد، قابل ردیابی باشه.
- README رو با بخش کوتاه «پاس ویرایش» به‌روز کن (چی اصلاح می‌شه، چی عمدا نمی‌شه، تاگل کجاست) و کامیت و پوش کن.

## معیار پذیرش

یه پاراگراف واقعی فارسی دیکته بشه: متن نهایی با نیم‌فاصله درست، ارقام فارسی، نقطه و ویرگول معقول سر جاش بشینه، بدون هیچ تغییر معنا، و کاربر هیچ تاخیر محسوسی حس نکنه. تاگل خاموش، رفتار قبلی برگرده. هر سه جزء آفلاین کار کنن.
